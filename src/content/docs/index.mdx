---
title: bubblegit
description: A fast, interactive git TUI that runs the real git binary.
template: splash
hero:
  tagline: A git TUI that stays fast on large repositories and is more interactive than the alternatives.
  image:
    file: ../../assets/bubblegit.svg
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/f3xp/bubblegit
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import Term from '../../components/Term.astro';
import wt from '../../generated/goldens/TestEndToEnd.html?raw';

```sh
go install github.com/f3xp/bubblegit/cmd/bubblegit@latest
cd your-repo && bubblegit
```

<Term html={wt} />

<CardGrid>
	<Card title="Runs the real git" icon="approve-check">
		Hooks, `commit.gpgsign`, credential helpers, `core.pager` and the rest of your git config work as they normally do.
	</Card>
	<Card title="Stage by file, hunk or line" icon="list-format">
		`space` stages what is under the cursor. `a` takes the whole hunk. `t` flips to the staged side to un-stage.
	</Card>
	<Card title="Fast on big repos" icon="rocket">
		Reads happen off the render loop and the log is paged, so the UI stays responsive.
	</Card>
</CardGrid>

:::note[Status: early]
bubblegit is at milestone 6. What these pages document works. Anything not documented is not built yet.
:::
